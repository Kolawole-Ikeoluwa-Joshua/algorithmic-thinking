"""Data Structures - case study: Arrays / Lists"""

new_list = [1, 2, 3]

"""

Operations on Data structures

"""

#1. Access and Read Values
result = new_list[0]

#2. Searching for an arbitrary value
if 1 in new_list: print('True')


"""
#3. Inserting values (shifts every element to the right) & Deleting values ( shifts every element to the left)
"""
numbers = []
len(numbers)


numbers.append(2)
numbers.append(200)


numbers.extend([4, 5, 6])


